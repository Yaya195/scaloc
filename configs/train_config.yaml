training:
  batch_size: 128
  learning_rate: 0.0005
  weight_decay: 0.00001
  epochs: 1
  device: auto
logging:
  print_every: 10
  save_every: 5
  eval_every: 5  # Evaluate on validation set every N rounds
